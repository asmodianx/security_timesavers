# NPO Security and CTI Requirements Wizard v4

The **Export CTI Architect JSON** button produces the exact top-level arrays expected by the CTI Intelligence Requirements Architect: `girs`, `fcrs`, `iers`, `dirs`, `pirs`, and `sirs`. Keep all files together and open `index.html` locally.
